jQuery ($) ->
  $(document).on 'change', '[data-select-place-url]', ->
    url = $('[data-select-place-url] option:selected').data 'customerPlaceUrl'

    if url
      $.getScript url
    else
      $('[data-place-type]').attr('value', '')
      $('[data-place-id]').html(
        $("<option></option>")
        .attr('value', '')
        .text('<%= I18n.t('helpers.select.prompt') %>')
        .attr('selected', true)
      )
      $('.customer_jobs_place label a').remove()
