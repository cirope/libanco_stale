$('table tbody').append('<%= j render partial: 'tbody', collection: @customers, as: :customer %>');
<% if @customers.next_page %>
  $('.pagination-container').html('<%= j will_paginate(@customers) %>');
<% else %>
  $('.pagination-container').remove();
<% end %>
