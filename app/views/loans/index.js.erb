$('[data-endless-container] > tbody').append(
  '<%= j render partial: 'tbody', collection: @loans, as: :loan %>'
);
<% if @loans.next_page %>
  $('.pagination-container').html('<%= j will_paginate(@loans) %>');
<% else %>
  $('.pagination-container').remove();
<% end %>
