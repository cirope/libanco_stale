<% if @payment.errors.any? %>
  $('form.edit_payment').replaceWith('<%= j render 'form' %>');
  $('#modal').find('[autofocus]').focus();
<% else %>
  $('#modal').modal('hide');
  $('#modal').remove();

  $('[data-payment=\"<%= @payment.id %>\"]').replaceWith(
    '<%= j render @payment %>'
  );

  selector = $('[data-loan-updateable-info]');
  selector.replaceWith('<%= j render(partial: 'loans/updateable_info') %>');
  selector.effect('highlight', 'slow');
<% end %>
