Hola <%= @user %>,
Éstos son los eventos para mañana <%= l(Date.tomorrow, format: :minimal) %>,
================================================================================

<% @schedules.each do |schedule| %>
  <%= l(schedule.scheduled_at, format: :time) %> | <%= schedule.description %>
  <% if schedule.schedulable %>
    <%= Customer.model_name.human(count: 1) %>: <%= schedule.label %>
    <%= Schedule.human_attribute_name :phone %>: <%= schedule.phones.first %>
    <%= Schedule.human_attribute_name :address %>: <%= schedule.address %>
  <% end %>
<% end %>

Podés organizar tus eventos en Libanco desde: <%= schedules_url(date: Date.tomorrow.to_s(:db)) %>
