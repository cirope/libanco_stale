if ($('#modal').length) {
  $('#modal .modal-body').html('<%= j render('form') %>');
} else {
  $('body').append('<%= j render('shared/modal', title: @title, body: render('form')) %>');
}
$('#modal').modal('show');
