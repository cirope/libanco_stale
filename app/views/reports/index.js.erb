$('[data-endless-container] > tbody').append(
  '<%= j render partial: @report.partial.singularize, collection: @collection, as: :record %>'
);
<% if @collection.next_page %>
  $('.pagination-container').html('<%= j will_paginate(@collection) %>');
<% else %>
  $('.pagination-container').remove();
<% end %>
