$('[data-calendar]').datetimepicker({
  altField: '#schedule_scheduled_at',
  showButtonPanel: false,
  altFieldTimeOnly: false,
  defaultDate: '<%= l (@schedule.scheduled_at.try(:to_date) || @current_date) %>',
  minDateTime: new Date()
});
