$('[data-place-type]').attr('value', 'Company');
$('select[data-place-id]').html(
  '<%= j options_for_select(
    companies_collection.unshift([t('helpers.select.prompt'), ''])
  ) %>'
).attr('disabled', false);
$('.customer_jobs_place label').html(
  '<%= j add_label('company', 'job') %>'
);
